<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:id="@+id/edit_note_frag"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        tools:context=".addoreditnote.EditNoteFragment">

        <include layout="@layout/edit_frag_main" />

        <include layout="@layout/bottom_sheet_layout" />

        <LinearLayout
            android:id="@+id/bottom_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:elevation="@dimen/bottom_layout_elevation"
            android:orientation="horizontal"
            app:layout_anchorGravity="bottom"
            app:layout_anchor="@id/options_bottom_sheet_layout"
            tools:targetApi="lollipop">

            <TextClock
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:fontFamily="@font/poppins"
                android:format12Hour="@string/date_time_string_format"
                android:textAlignment="center" />

            <ImageView
                android:id="@+id/bottom_sheet"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="8dp"
                android:contentDescription="@string/menu_icon_desc"
                app:srcCompat="@drawable/ic_more_bottom_sheet" />

        </LinearLayout>

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

</layout>